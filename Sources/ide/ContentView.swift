import SwiftUI

struct ContentView: View {
    var body: some View {
        GhosttyTerminalView()
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}

#Preview {
    ContentView()
}
